import React from "react";

const CandelarEvent = ({ event }) => {
    const { title } = event;
    return (
        <div>
            {/* <strong>{title} - </strong> */}
        </div>
    );
};

export default CandelarEvent;
